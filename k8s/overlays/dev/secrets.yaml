# Dev 환경: K8s Secrets 직접 사용
apiVersion: v1
kind: Secret
metadata:
  name: ledger-service-secrets
  labels:
    app: ledger-service
    environment: dev
type: Opaque
stringData:
  DB_USERNAME: "${DB_USERNAME}"
  DB_PASSWORD: "${DB_PASSWORD}"
