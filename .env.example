# ========================================
# Ledger Service 환경 변수 설정
# ========================================
#
# 사용 방법:
# 1. 이 파일을 .env로 복사: cp .env.example .env
# 2. 실제 환경에 맞게 값 수정
# 3. .env 파일은 Git에 커밋하지 마세요!

# ========================================
# 포트 설정 (Ledger Service 전용)
# ========================================
# Ledger Service: Backend 8082, Frontend 3002, gRPC 9092, MySQL 3308
# Auth Service: Backend 8081, Frontend 3001, gRPC 9091, MySQL 3307
# Boilerplate: Backend 8080, Frontend 3000, gRPC 9090, MySQL 3306

# Backend HTTP 포트
BACKEND_PORT=8082

# Frontend 개발 서버 포트 (docker-compose)
FRONTEND_PORT=3002

# gRPC 서버 포트
GRPC_SERVER_PORT=9092
GRPC_PORT=9092

# ========================================
# Database Configuration
# ========================================
DB_HOST=localhost
DB_PORT=3308  # Ledger Service는 3308 사용
DB_NAME=hamkkebu_ledger
MYSQL_ROOT_PASSWORD=root

# ========================================
# JWT Configuration
# ========================================
# IMPORTANT: Change in production! Must be at least 64 characters for HS512
JWT_SECRET=ThisIsADevelopmentSecretKeyPleaseChangeInProductionAtLeast64Characters

# ========================================
# Redis Configuration (공유)
# ========================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=""

# ========================================
# Application Configuration
# ========================================
SPRING_PROFILES_ACTIVE=dev
